{
  "name": "kairo-lite-api",
  "version": "1.0.0",
  "description": "Servicio para reportes de métodos de pago unificados",
  "main": "handler.js",
"scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy-prod": "export AWS_PROFILE=mokari-kairo && sls deploy",    
    "local": "AWS_PROFILE=mokari-kairo ./node_modules/.bin/nodemon --watch ../.. --ext js,yml --exec './node_modules/.bin/serverless offline'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.687.0",
    "jsonwebtoken": "^9.0.2",
    "jwk-to-pem": "^2.0.6",
    "lambda-warmer": "^2.2.0",
    "mysql2": "^3.6.5"
  },
  "devDependencies": {
    "serverless-offline": "^14.3.4",
    "nodemon": "^2.0.22"
  }
}
